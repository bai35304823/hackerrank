package hackerrank;

public class sql_least_earning_location_count {
	/*
	 * select c.name, SUM(r.fare) from CITIES c left join USERS u on c.id=u.city_id
	 * left join RIDES r on u.id=r.user_id
	 */
}
